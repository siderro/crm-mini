// Contacts module — placeholder (standalone, replaces CRM contacts)

export function renderContacts(container) {
  container.innerHTML = `
    <div class="module-placeholder">
      <div class="module-placeholder-title">Contacts</div>
      <div class="module-placeholder-desc">People, notes, export — coming soon</div>
      <a href="#/" class="module-placeholder-back">← Back to home</a>
    </div>
  `;
}
