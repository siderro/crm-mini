{
  "name": "crm-mini",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^6"
  }
}
